Modulo re.

	>>> import re
	String de teste:
	>>> a = "Está tudo bem."

- fullmatch: Expressão regular e string
	Ex:
	>>>re.fullmatch(r'Está',a) -> nao dá match, não retorna
	>>>re.fullmatch(r'Está tudo bem.',a)
	<_sre.SRE_Match object; span=(0,14), match='Está tudo bem.'>
	>>>re.fullmatch(r'Está tudo bem.',a).span()
	(0,14)

- search: Expressão regular e string
	procura a expressão regular na string, retornando a primeira ocorrência
	Ex:
	>>> re.search(r'oi',a) -> quando nao encontra não retorna
	>>> re.search(r'tudo',a)
	<_sre.SRE_MATCH objetct; span=(5,9), match='tudo'>
	>>> re.search(r'tudo',a).span()
	(5,9)
	>>> re.search(r'tudo',a).group()
	tudo

- sub : Expressão regular, string(substituição) e string(original)
	Substitui ocorrencias do padrão encontrado na string 
	Ex: 
	>>> re.sub(r'tudo', 'nada', a)
	'Está nada bem'

- subn : O mesmo que o sub, mas retorna o numero de substituições feitas
	Ex:
	>>> re.subn(r'tudo', 'nada', a)
	('Está nada bem.', 1)

- split : Expressão regular e string
	Retorna uma lista de strings separadas pela expressão regular
	Ex:
	>>> re.split(r'\s+',a)
	['Está','tudo','bem.'] 


- findall: Expressão regular e string
	Retorna uma lista de strings correspondentes as ocorrências da expressão
	Ex:
	>>> re.findall(r'\w+',a)
	['Está','tudo','bem']


\W -> tudo menos letras e numeros
\w -> letras
